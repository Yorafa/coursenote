因为贫穷，所以在研究如何最大化利用一台服务器。对于我来说就是用一台服务器部署所有的应用。但我们都知道端口就那么几个，如果要用http/https那就两个端口(80, 443)，怎么可能和其他的应用一起使用呢？

反向代理就解决了这种问题，利用反向代理，我们可以将二级域名指向对应端口而非锁死的80和443。也就是说访问`sub.example.com`理应是访问`sub.example.com`所对应服务器的80端口，但经过反向代理，访问的可以是任意其他未被占用、自己可以选择的端口。